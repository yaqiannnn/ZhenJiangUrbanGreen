<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nju.urbangreen.zhenjiangurbangreen">

    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".util.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_zhengjianglvhua"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/myAppTheme">

        <activity android:name=".map.MapActivity" />
        <activity android:name=".search.SearchActivity" />
        <activity android:name=".navigation.NaviActivity" />
        <activity android:name=".settings.SettingsActivity" />
        <activity android:name=".events.EventsActivity"></activity>
        <activity android:name=".maintainRecord.MaintainInfoActivity"></activity>
        <activity android:name=".maintainRecord.MaintainListActivity"></activity>
        <activity android:name=".inspectRecord.InspectInfoActivity"></activity>
        <activity android:name=".inspectRecord.InspectListActivity"></activity>
        <activity
            android:name=".startup.WelcomeActivity"
            android:theme="@style/TranslucentTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".startup.LoginActivity" />

        <service
            android:name=".util.DownloadNewApkService"
            android:enabled="true"
            android:exported="true" />
    </application>

</manifest>